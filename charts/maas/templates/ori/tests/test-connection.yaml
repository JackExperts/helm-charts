apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "maas.fullname" . }}-test-connection"
  labels:
    {{- include "maas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "maas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
